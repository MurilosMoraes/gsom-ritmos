<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links: intercepta https://gdrums.com.br/* e abre direto no app
                 em vez de cair no navegador. Requer /.well-known/assetlinks.json
                 no domínio com o fingerprint SHA-256 do keystore de release.
                 autoVerify=true: Android valida o assetlinks no primeiro boot e
                 marca como "verified" → abre direto sem perguntar. Sem isso
                 mostraria "Abrir com..." toda vez. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="gdrums.com.br" />
                <data android:pathPrefix="/login" />
                <data android:pathPrefix="/plans" />
                <data android:pathPrefix="/payment-success" />
                <data android:pathPrefix="/admin" />
            </intent-filter>

        </activity>

        <!-- FCM default notification channel: quando um push chega sem
             android_channel_id explícito, FCM usa este ID. Sem isso,
             Android 8+ silencia o push automaticamente (problema antes:
             "FCM accepted, device received nothing"). Channel é criado
             pelo @capacitor/push-notifications no init do app. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="gdrums-default" />

        <!-- ForegroundService de áudio: mantém WebView vivo + AudioContext rodando
             em background. mediaPlayback é uma das categorias que SO permite rodar
             indefinidamente (Doze mode não mata). Notification persistente
             mostra "GDrums tocando" com botão de Pause/Stop. -->
        <service
            android:name=".GDrumsAudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false"
            android:stopWithTask="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- ForegroundService pra background audio. FOREGROUND_SERVICE_MEDIA_PLAYBACK
         obrigatória em Android 14+ (API 34) — sem ela = MissingForegroundServiceTypeException. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Notification do FGS: Android 13+ exige permission runtime. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Bluetooth (pedal) — Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
                     android:usesPermissionFlags="neverForLocation" />
    <!-- Login por biometria (@capgo/capacitor-native-biometric) — BiometricPrompt.
         Android 9+ usa USE_BIOMETRIC; USE_FINGERPRINT é o fallback pra Android 6-8. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
</manifest>
